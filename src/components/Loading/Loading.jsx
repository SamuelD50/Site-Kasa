import './Loading.scss'

/* This component is used to create a loader when the page is loading */

function Loading() {

    return (
        <span className="loader"></span>
    )
}

export default Loading